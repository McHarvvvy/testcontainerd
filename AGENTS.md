# AGENTS.md
本文件面向在本仓库工作的智能编码代理，提供简化版协作规范。

## 仓库概览
- 语言：Go（`go.mod` 当前为 `go 1.24.13`）
- 模块：`github.com/McHarvvvy/testcontainerd`
- 目标：为 Go 集成测试提供可复用的 daemon/client 容器运行时
- 核心包：`client`、`daemon`、`container`、`protocol`、`tcdruntime`
- 运行依赖：完整集成链路依赖 Docker Engine

## 开发与验证（简化说明）
- 使用 Go 生态的常规方式进行构建、格式化、静态检查和测试。
- 修改完成后，至少确保：能编译、基础检查通过、测试结果可接受。
- 本仓库当前几乎无测试用例，测试更多是编译与链接层面的回归校验。

## 代码风格

### 格式与文件组织
- 保持 `gofmt` 风格一致。
- 文件按职责拆分，避免单文件承载过多逻辑。
- 平台差异放在 build tag 文件中（如 `*_windows.go`、`*_unix.go`）。

### 导入规范
- 导入分组顺序：标准库 → 仓库内包 → 第三方包。
- 仅在必要时使用别名（例如避免冲突或增强语义）。
- 不使用 dot import，避免无用导入。

### 命名约定
- 导出符号使用 PascalCase，命名体现领域语义。
- 非导出符号使用 camelCase。
- 构造函数使用 `NewX`，允许 panic 的辅助函数使用 `MustNewX`。
- 选项函数使用 `WithX` 风格。
- 缩写风格保持与现有代码一致（如 SUT、TTL、PID）。

### 类型与 API 设计
- 优先使用配置结构体，避免过长的位置参数。
- 公共 API 保持稳定、收敛；复杂编排下沉到内部实现。
- 扩展点优先接口抽象（如 `SUTBootPlan`、`Registrar`）。
- 协议与配置载体优先使用清晰的结构体。

### 错误处理
- 默认返回错误，不滥用 panic（仅限 `Must*` 场景）。
- 下层错误向上返回时补充上下文。
- 对外错误语义保持稳定、可识别。
- 使用早返回降低嵌套层级。
- 清理路径中的非关键错误可显式忽略。

### 日志
- 使用 `log.Printf` 记录关键运行事件。
- 日志应包含必要上下文，便于排障。
- 禁止输出敏感信息（token、密钥、凭据等）。

### 并发与生命周期
- 共享状态通过 `sync.Mutex` 或 `atomic` 保护。
- 锁内避免长耗时或阻塞操作。
- IO/网络/进程操作应带 context 与超时控制。
- 关闭与回收流程保持幂等（如 `sync.Once`、安全重试）。
- 并发启动场景保持单拥有者语义（文件锁 + 等待/重试）。

### 注释与文档
- 注释优先解释“为什么”，而非“做了什么”。
- 导出符号注释保持简洁准确。
- 关键决策建议就近记录，方便后续维护。

## 变更边界
- 尽量只改动与任务直接相关的代码。
- 避免在同一提交中混入无关重构。
- 若行为、配置或接口语义发生变化，应同步更新文档。

## 代理交付检查清单
1. 确认修改后的代码风格与现有仓库一致。
2. 确认没有引入明显并发/生命周期回归风险。
3. 确认接口与协议语义未被意外破坏。
4. 确认关键路径有基础验证结果支撑。
5. 确认文档与实现保持一致。
